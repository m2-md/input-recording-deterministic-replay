{
  "name": "input-recording-deterministic-replay",
  "version": "1.0.0",
  "description": "Kara kutu: girdi kaydı ve deterministik replay (Kanyon mini oyunu) — RLE kayıt formatı, hash izi, desync avı",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "fixture": "vite-node scripts/make-fixture.ts",
    "bench": "vite-node scripts/bench.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vite-node": "^2.1.0",
    "vitest": "^2.1.0"
  }
}
