{
  "name": "input-recording-deterministic-replay",
  "version": "1.0.0",
  "description": "Black-box input capture and deterministic replay harness for canvas games: compact RLE recording format, state checksum hashing, and desync regression testing.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "fixture": "vite-node scripts/make-fixture.ts",
    "bench": "vite-node scripts/bench.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vite-node": "^2.1.0",
    "vitest": "^2.1.0"
  }
}
